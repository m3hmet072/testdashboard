# Garage Dashboard Update Log

Date: 2026-03-20

## Summary
This update focused on CSS cleanup, theme-token consolidation, and removing dynamic garage branding color overrides.

## What changed
- Consolidated design tokens to a single source in `src/styles/theme.css`.
- Removed duplicate `:root` token declarations from `src/styles/base.css`.
- Kept dark mode tokens in the same theme file (`:root[data-theme="dark"]`).
- Deleted unused duplicated stylesheet `src/styles/global.css`.
- Cleaned duplicate/shared style blocks across page stylesheets (notably dashboard/request-board related shared chips/status styles).
- Disabled runtime garage brand color overriding in `src/utils/branding.js`.
- Removed hardcoded garage color defaults from `src/services/garageService.js`.

## Result
- All style modules now consume shared CSS variables from `src/styles/theme.css`.
- Runtime color branding overrides are no longer applied.
- Build passes after the refactor.

## Validation
- Verified all entry files import `src/styles/theme.css`.
- Verified only theme roots remain in `src/styles/theme.css`.
- Ran production build successfully (`npm run build`).